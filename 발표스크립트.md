# 날씨 앱 컴포넌트 분리 — 발표 스크립트

(예상 소요 시간: 약 3~4분 / 화면에 http://localhost:5173 띄워둔 상태로 시작)

---

## 1. 오프닝 — 왜 나눴나 (30초)

"안녕하세요. 이번 과제는 기존에 만들어둔 날씨 목업 하나짜리 컴포넌트를, **기능 변경 없이** 4개의 재사용 가능한 컴포넌트로 쪼개는 작업이었습니다.

화면은 이전과 똑같습니다 — 도시 검색하고, 카드 누르면 상태바에 표시되고, 상세보기 누르면 알림이 뜨죠. 달라진 건 겉모습이 아니라 **내부 구조**입니다."

*(여기서 화면 보여주며 검색 한 번, 카드 클릭 한 번, 상세보기 한 번 시연)*

---

## 2. 전체 구조 소개 (1분)

"구조는 이렇게 4단계로 나눴습니다."

*(코드 에디터로 전환, `src/components` 폴더 보여주기)*

- **`WeatherParent.vue`** — 앱의 두뇌 역할입니다. `weatherList`, `searchQuery`, `statusMessage` 같은 반응형 상태를 전부 여기서 관리하고, 검색 필터링(`computed`)도 여기 있습니다.
- **`BaseDashboardCard.vue`** — 흰 카드에 그림자 주는 디자인만 담당하는 껍데기입니다. `<slot>` 하나로 내용물을 갈아끼울 수 있어서, "도시 검색" 박스에도 쓰고 "지역별 날씨 현황" 박스에도 똑같이 재사용했습니다.
- **`SearchBar.vue`** — 검색창 자체입니다. 부모에게서 `searchQuery`를 `props`로 받아 화면에 보여주고, 사용자가 타이핑하면 `update-query`라는 이벤트로 새 검색어를 부모에게 올려보냅니다.
- **`WeatherCard.vue`** — 카드 한 장입니다. `city` 객체 하나를 `props`로 받고, 클릭하면 `select-card`, 상세보기 버튼을 누르면 `click-detail` 이벤트를 부모로 보냅니다.

"정리하면 **데이터는 위에서 아래로(props), 이벤트는 아래에서 위로(emit)** 흐르는 구조입니다."

---

## 3. 핵심 포인트 — 슬롯인데 왜 부모가 직접 통신하나 (1분)

*(WeatherParent.vue 템플릿 코드 보여주기)*

"여기서 한 가지 재밌는 부분이 있는데요. `SearchBar`랑 `WeatherCard`를 코드 상에서는 `BaseDashboardCard` **태그 안에** 넣었습니다. 시각적으로는 분명히 `BaseDashboardCard`의 slot 안에 들어가 있는 거예요.

그런데 `:search-query`, `@update-query` 같은 props/이벤트 바인딩은 `BaseDashboardCard`가 아니라 **`WeatherParent`가 직접** 하고 있습니다. `BaseDashboardCard`는 `SearchBar`가 뭔지, `WeatherCard`가 뭔지 전혀 모릅니다.

이게 가능한 이유는, slot으로 전달되는 자식 콘텐츠는 **시각적으로는 자식(BaseDashboardCard) 내부에 배치**되지만, **스크립트적으로는 원래 있던 부모(WeatherParent)의 스코프에서 컴파일되고 평가**되기 때문입니다. 그래서 `BaseDashboardCard`는 그냥 '내용물이 뭐가 오든 예쁜 흰 박스로 감싸주는' 순수 레이아웃 컴포넌트로 남을 수 있고, 실제 로직 연결은 전부 `WeatherParent`가 담당하는 깔끔한 역할 분리가 됩니다."

---

## 4. 스타일 분리 (30초)

"CSS도 그냥 한 덩어리로 두지 않고, 각 컴포넌트의 `<style scoped>` 안에 그 컴포넌트가 필요한 스타일만 남겼습니다. 예를 들어 카드 색깔·배지 색은 `WeatherCard.vue` 안에만 있고, 검색창 스타일은 `SearchBar.vue` 안에만 있어서, 나중에 카드 디자인만 바꾸고 싶을 때 다른 컴포넌트를 건드릴 필요가 없습니다."

---

## 5. 마무리 (30초)

"결과적으로 화면에 보이는 동작—검색, 카드 클릭, 상세보기—은 리팩터링 전과 완전히 동일하고, 코드만 역할별로 4개 파일로 나뉘었습니다. props/emits로 데이터 흐름을 명확히 하고, slot으로 레이아웃과 로직을 분리한 게 이번 과제의 핵심이었습니다.

질문 있으실까요?"

---

## 예상 질문 대비 메모

- **Q. WeatherCard가 emit할 때 city 객체를 통째로 넘기는 이유는?**
  A. 원래는 `(cityName, status)` 두 개 인자로 넘겼는데, 컴포넌트로 분리하면서 `city` 객체 하나로 단순화했습니다. 최종적으로 alert에 찍히는 문구는 동일합니다.

- **Q. BaseDashboardCard에 title prop을 넣은 이유는, 원래 없던 기능 아닌가?**
  A. 데이터/로직 변경은 없고, "검색박스와 리스트박스 디자인을 공통화"하라는 요구사항에 맞춰 두 박스를 구분하는 최소한의 시각적 라벨만 추가했습니다.

- **Q. 왜 Vue CDN 방식이 아니라 Vite 프로젝트로 바꿨나?**
  A. `<style scoped>`와 `.vue` 단일 파일 컴포넌트는 브라우저가 직접 해석할 수 없고 컴파일 과정이 필요해서, 이번 과제부터는 Vite 빌드 환경을 사용했습니다.
