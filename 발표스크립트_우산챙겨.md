# 온도 단위 토글 + 우산 챙겨 기능 — 발표 스크립트

(예상 소요 시간: 약 5분 / 화면에 http://localhost:5173 띄워둔 상태로 시작, 홈 화면부터)

---

## 0. 오프닝 (20초)

"안녕하세요. 오늘은 기존 날씨 목업 프로젝트에 두 가지를 추가했습니다.

첫째, **Pinia 스토어를 활용한 온도 단위(℃/℉) 토글 기능**이고, 둘째는 신규 기능으로 만든 **'우산 챙겨'** — 위치 기반으로 비 소식과 외출 시점을 알려주는 기능입니다. 순서대로 데모하면서 설명드리겠습니다."

---

## 1. 온도 단위 토글 (1분 20초)

*(네비게이션 바 오른쪽의 ℃ 버튼을 가리키며)*

"상단 네비게이션 바 옆에 이 토글 버튼이 있습니다. 눌러보면—"

*(버튼 클릭 → 홈 화면 카드들의 온도가 즉시 화씨로 바뀌는 걸 보여줌)*

"—홈 화면 카드 전체와, 상세 페이지 온도까지 한 번에 바뀝니다.

핵심은 **Pinia 스토어 하나로 상태를 중앙 관리**했다는 겁니다. `configStore.js`에 세 가지만 정의했습니다.

- **state**: `unit` — 초기값 `'celsius'`
- **getter**: `unitSymbol` — 현재 단위에 맞는 기호(℃/℉)를 반환
- **action**: `toggleUnit` — celsius와 fahrenheit를 서로 바꿔주는 함수

그리고 온도를 보여주는 쪽(`WeatherCard.vue`, `WeatherDetailView.vue`)에서는 각자 `computed`로 이렇게 계산합니다."

*(코드 잠깐 보여주기)*

```js
const displayTemp = computed(() => {
  const rawTemp = props.city.temp // 원본은 항상 섭씨 숫자로 저장
  if (configStore.unit === 'fahrenheit') {
    return Math.round((rawTemp * 9) / 5 + 32)
  }
  return rawTemp
})
```

"원본 데이터는 항상 섭씨로만 저장해두고, **보여줄 때만 변환**하는 방식이라 데이터 자체는 건드리지 않습니다. 여러 화면에서 똑같은 변환 로직이 반복되는 건, 사실 Composable로 뽑아내면 더 깔끔해지는데 — 이번 과제 범위에서는 제외하고 각 컴포넌트에 두었습니다."

---

## 2. 우산 챙겨 기능 (2분 50초)

*(상단 네비게이션에서 "☂️ 우산 챙겨" 클릭)*

"두 번째는 신규 기능입니다. 문제의식은 이거였어요 — 일반적인 날씨 예보는 '동 단위'라서, 출근길에 갑자기 오는 소나기는 제때 못 잡습니다. 그래서 위치 기반으로 '비 오기까지 몇 분'을 알려주는 화면을 만들었습니다."

### 2-1. 데이터 연결 (40초)

"위쪽 '위치 설정' 카드부터 보시면, 도시 4개 버튼(서울/부산/인천/성남)이 있는데요."

*(도시 버튼 하나 클릭 → 카운트다운/게이지/타임라인이 그 즉시 바뀌는 걸 보여줌)*

"이게 홈 화면에서 봤던 것과 **같은 `weatherMock.js` 데이터**입니다. 각 도시 객체에 `rain`이라는 필드를 추가해서 강수 카운트다운, 우산 필요 지수, 60분 강수 추이까지 다 들어있어요. 그래서 이 화면 혼자 노는 가짜 데이터가 아니라, 앱 전체가 하나의 데이터 소스를 공유하는 구조입니다."

### 2-2. 실제 위치 연동 (1분)

"'내 위치 사용' 버튼을 누르면—"

*(버튼 클릭 → 위치 권한 팝업 → 허용)*

"—두 가지 일이 동시에 일어납니다.

첫째, 브라우저 `navigator.geolocation`으로 **실제 GPS 좌표**를 받아서, 4개 도시 중 가장 가까운 곳의 날씨 데이터를 매칭합니다. 둘째, 그 좌표를 **카카오 로컬 API**로 보내서 실제 도로명 주소로 변환해 화면에 보여줍니다.

정리하면 — **위치 자체는 진짜**고, **거기 붙는 날씨는 4개 mock 도시 중 근사치**입니다. 실제 기상청 API까지 붙이면 완전히 실제 데이터가 되는데, 그건 다음 단계로 남겨뒀습니다."

### 2-3. 외출 감지 (1분 10초)

*(아래로 스크롤해서 "외출 감지" 카드로 이동)*

"마지막은 '외출 감지' 기능인데요. 실제 앱이라면 Wi-Fi 연결이 끊기는 순간으로 '집에서 나갔다'를 감지하는데, **브라우저는 Wi-Fi 상태에 접근할 수 없어서** 대안으로 GPS를 씁니다.

'현재 위치를 집으로 등록' 버튼을 누르면 그 좌표를 저장하고, 이후로는 `watchPosition()`으로 실시간 위치를 계속 추적합니다."

```js
navigator.geolocation.watchPosition((position) => {
  const dist = haversineDistanceKm(homeLat, homeLng, position.coords.latitude, position.coords.longitude)
  if (dist > 0.15 && wasInsideRadius) {
    triggerLeaveToast() // 반경을 벗어난 순간 = 외출로 간주
  }
})
```

"집 좌표 기준 **반경 150m를 벗어나는 순간**을 '외출'로 간주해서 자동으로 팝업을 띄웁니다."

*(수동 시뮬레이션 버튼을 눌러 토스트가 뜨는 걸 보여줌)*

"실제로 150m를 걸어야 테스트되는 기능이라, 발표에서는 수동 버튼으로도 같은 팝업을 재현할 수 있게 남겨뒀습니다."

---

## 3. 마무리 (20초)

"정리하면, Pinia 스토어로 앱 전역 상태(온도 단위)를 관리하는 패턴과, GPS·카카오 API 같은 브라우저 네이티브 기능을 실제로 연동해보는 것까지 이번 과제에서 다뤘습니다. 감사합니다."

---

## 예상 질문 대비 메모

- **Q. 우산 챙겨 화면의 비 예보는 실제 데이터인가요?**
  A. 아닙니다. `weatherMock.js`에 있는 4개 도시의 mock 데이터입니다. 실제로 연동하려면 기상청 초단기예보 API가 필요하고, 브라우저에서 직접 호출 시 CORS 문제가 있어 프록시 서버가 필요합니다.

- **Q. 왜 Wi-Fi 대신 GPS로 외출을 감지하나요?**
  A. 브라우저는 보안/프라이버시 정책상 연결된 Wi-Fi의 SSID나 연결 상태를 읽는 API 자체가 없습니다. 네이티브 앱(Flutter/RN)이라면 `WifiManager`(Android), `NEHotspotNetwork`(iOS) 같은 API로 실제 Wi-Fi 감지가 가능합니다.

- **Q. 온도 단위는 왜 Pinia로 관리했나요, props로 넘기면 안 되나요?**
  A. 온도를 표시하는 컴포넌트가 홈 카드, 상세 페이지, 우산 챙겨 화면까지 여러 곳에 흩어져 있어서 props로 계속 내려보내는(props drilling) 대신, 전역에서 한 번에 구독하는 Pinia 스토어가 더 적합했습니다.

- **Q. 카카오 API 키는 어디서 관리하나요?**
  A. `.env.local`에 `VITE_KAKAO_APP_KEY`로 저장하고, git에는 커밋되지 않도록 `.gitignore`에 등록했습니다. 카카오 JS 키는 도메인 등록 기반으로 제한되는 방식이라 클라이언트에 노출되는 게 원래 설계입니다.
